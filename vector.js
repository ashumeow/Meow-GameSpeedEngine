var vector = function(X2d) {
	'use strict';
	var meow = this;
	X2d.vector = function(x, y) {
		meow.x = x || 0;
		meow.y = y || 0;
	};
	xx.extend(vector, {
		zero: new X2d.vector()
	});
	xx.extend(vector.prototype, main.events, {

		//
		// Still more to code
		//

	});
};
